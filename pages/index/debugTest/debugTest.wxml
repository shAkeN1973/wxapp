<view class="cu-bar margin-top bg-white">
  <view class="action">
    <text class="cuIcon-title text-blue"></text>选择代表药仓的按钮
  </view>
</view>
  <view class="grid col-5 padding-sm">
    <view class="margin-tb-sm text-center" wx:for="{{numberList}}" wx:key="indeX" id="{{index}}">
     <button class="cu-btn round bg-red shadow}}" bindtap="selectRoom" id="{{item.number}}">{{item.number}}</button>
     </view>
  </view>

<view class="padding flex flex-direction">
  <button class="cu-btn bg-red margin-tb-sm lg" bindtap="beginDebug">开始</button>
</view>

<view class="cu-bar margin-top bg-white">
  <view class="action">
    <text class="cuIcon-title text-red"></text>当前选择的药仓是：{{selectNumber}}
  </view>
</view>




